name: bluefors #Environment name
dependencies:
  - python=3.6.9
  - pip:
    - dash
    - dash-renderer
    - dash-core-components
    - dash-html-components
    - plotly
    - gunicorn # for app deployment